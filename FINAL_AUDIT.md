## Findings

| ID | Severity | Evidence | Affected behavior | Affected invariant | Required correction | Blocks completion |
|---|---|---|---|---|---|---|
| FA-1 | High | `scripts/lib/chat.py:Seed.commit` leaves the exclusively created file after write/fsync failure; `/tmp/uncle-verification/MC-007.log` records “residual seed True identity None” and failed retry. DEFECTS.md D-3 remains unresolved. | Failed seed creation prevents subsequent Start despite retaining the draft. | CHANGE_PLAN.md Error and recovery behavior; AC-6. | Recover failed writes without overwriting competing files or launching partial intake; add fault-injection regression coverage. | YES |
| FA-2 | High | `scripts/lib/chat.py:Conversation.commit` validates sections before `Seed.commit` sanitizes them; `/tmp/uncle-verification/MC-007.log` demonstrates acceptance of a control-only Summary. DEFECTS.md D-2 remains unresolved. | The commit API can persist a brief with an empty sanitized Summary. | I-4; CHANGE_PLAN.md requires nonempty substantive seed sections. | Sanitize before validating required sections; test that rejected sanitized content creates no seed. | YES |
| FA-3 | High | `uncle_tui.py:1916` excludes character codes at or above `curses.KEY_MIN`; `_run_loop:2972` uses byte-oriented `getch`. `/tmp/uncle-verification/MC-009.log` records “é中😀” becoming “é” in the key harness. | Chat silently loses supported Unicode input; the real terminal path also lacks Unicode decoding. | AC-2 text acceptance; MANUAL_CHECKLIST.md MC-009. | Use Unicode-aware terminal input while distinguishing special keys; verify non-ASCII text through a real PTY and the composer. | YES |
| FA-4 | Medium | `scripts/lib/chat.py:103` emits `@a@b` for filename `a@b`, but `_REFERENCE` splits unquoted references at `@`; `/tmp/uncle-verification/MC-009.log` confirms that emitted value. | A picker-selected readable file cannot subsequently be submitted as its intended reference. | AC-3/4; shared resolver and usable reference insertion. | Quote parser-significant filenames or reject them inline; test picker insertion followed by payload expansion. | YES |
| FA-5 | Medium | `uncle_tui.py:_project_root` preserves absolute environment roots; `scripts/lib/chat.py:References.open` opens that root with `O_NOFOLLOW`. `/tmp/uncle-verification/MC-011.log` records failure for an explicit symlink project root. | Chat references and seed creation fail when the configured project root is a directory alias. | I-1/5; AC-3/4/6. | Resolve the trusted project-root alias while retaining no-follow checks beneath it; test references and Start through the launcher. | YES |
| FA-6 | Medium | CHANGE_TEST_REPORT.md R-1 claims the incoming baseline had 22 passing tests. The cited report supplies no raw baseline result; `.uncle/workflow/logs/implementation.jsonl` contains tool-use markers and narrative only. `/tmp/uncle-verification/MC-001.log` establishes the final 23-test run only. | The historical baseline PASS claim cannot be tied to executed evidence. | Auditable before/after verification. | Supply the original 22-test execution output and revision, or label that historical claim unverified. | YES |
| FA-7 | High | `uncle_tui.py:1893,1908` unconditionally reject Brief, Query and Fork; native dispatch and its test are absent. `.uncle/workflow/waivers/AC-5`, `AC-8`, `AC-9`, and `AC-10` record deferral to another issue. | Model-assisted intake and running-model interaction remain unavailable; full acceptance and regression verification remain incomplete. | AC-5/8/9/10; I-6; CHANGE_PLAN.md V-1–V-4. | Retain the recorded deferrals and implement and verify these obligations in the follow-up; do not describe this delivery as complete chat inference. | NO |

NOT READY