# Uncle per-stage model config.
# Format: STAGE MODEL  (STAGE = stage log name, `reviewer`, or `runner`;
# MODEL = cline model id, or a runner name for `runner`).
runner cline
project-plan Codex
updated-plan Claude
implementation Codex
change-spec DeepSeek V4 Pro
change-plan Codex
updated-change-plan Claude
