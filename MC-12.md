- Check ID: MC-12
- Action actually performed: Ran `git checkout -- uncle_tui.py scripts/tests/tui-session-panel-test.sh` to rollback to baseline, then ran `bash scripts/tests/tui-session-panel-test.sh` to verify TUI panel tests pass exactly as in baseline. Also verified `[failed]` suffix is present on failed-worker rollup lines via `grep -n '\[failed\]' uncle_tui.py`.
- Expected result: TUI panel tests pass exactly as in baseline (no regression). `[failed]` suffix restored on failed-worker rollup lines.
- Actual result: All 22 TUI panel tests passed (same count as baseline). `grep` returned 1 hit at line 5478: `title += " [failed]"`, confirming the `[failed]` suffix is restored.
- Evidence:
  - `git checkout` completed successfully (changes to both files reverted, working tree clean of those two files).
  - `bash scripts/tests/tui-session-panel-test.sh` output: `......................` (22 dots) + `Ran 22 tests in 0.011s` + `OK`.
  - `grep -n '\[failed\]' uncle_tui.py` returns: `5478:                title += " [failed]"` (1 hit).
  - After evidence capture, `git stash pop` restored the working tree to the pre-rollback state.
- Status: PASS
- Defect reference: None
