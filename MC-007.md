# MC-007 Evidence

## Check ID
MC-007

## Action Actually Performed
Executed automated test suites covering AC-9 (Cline catalogue, billing, self-hosted unchanged) and inspected source code invariants. The manual TUI-interactive steps (configure cline stage, open picker, switch billing catalogues, configure self-hosted, compare against baseline) could not be performed in this non-interactive environment.

## Expected Result
Cline ids same as before. Billing switch drops cross-catalogue model. Self-hosted shows discovered models. Cline model row unchanged.

## Actual Result
Automated test evidence confirms all AC-9 behaviors pass:
- `bash scripts/tests/tui-config-test.sh` → exit 0, "all checks passed" (104 field/defaults, 29 round-trip, 125 dormant/rollback checks)
  - Verifies cline model catalogue uniqueness (line 269: "catalogue ids are unique across the vendor lists")
  - Verifies billing switch drops cross-catalogue model (lines 537-546)
  - Verifies self-hosted picker accepts custom ids (lines 172-186)
  - Verifies cline model row field set matches pre-change expectations (lines 146-149)
  - Verifies billing catalogues (CLINEPASS vs CLINE_USAGE) and model_suits_billing
  - Verifies self-hosted picks up discovered models
- `bash scripts/tests/stage-runner-test.sh` → exit 0, "22 checks passed" (targeted AC-9)
  - Verifies cline model is forwarded as `--model` flag
  - Verifies self-hosted model forwarding
  - Verifies model is dropped when runner is non-cline

Source code invariants confirmed (uncle_tui.py):
- Cline catalogue lists (MODEL_CATALOG_CLINEPASS at line 203, MODEL_CATALOG_USAGE_PAID at line 229) unchanged
- CLINEPASS/CLINE_USAGE billing constants (line 300) unchanged
- model_catalog(), billing_of_model(), model_suits_billing() functions unchanged
- Self-hosted model discovery path unchanged
- BILLING_CHOICES unchanged
- DEFAULT_CLINE_MODEL unchanged (`cline-pass/deepseek-v4-pro`)
- valid_model_id() for cline runner unchanged
- stage_fields for cline returns `["runner", "effort", "billing", "model"]` unchanged

## Evidence
1. `bash scripts/tests/tui-config-test.sh` exit code 0, "all checks passed" (executed 2026-09-19T19:32Z)
2. `bash scripts/tests/stage-runner-test.sh` exit code 0, "22 checks passed"
3. CHANGE_TEST_REPORT.md confirms AC-9 passes via both suites
4. Source inspection of uncle_tui.py confirms cline catalogue, billing model, self-hosted discovery, and model row structurally unchanged

## Status
BLOCKED-HUMAN

## Defect Reference
None

## Blocked Reason
Two blockers prevent full manual execution:
1. **Dependency on MC-003**: MC-007 declares `Depends on: MC-003` (Custom id validation per runner). MC-003 was not assigned to this worker. The checklist-groups barrier (group 3 → group 4) is unsatisfied.
2. **TUI interactive required**: The manual check calls for visually configuring a cline stage, opening pickers, switching billing catalogues, and comparing against pre-change baseline. This requires an interactive curses terminal with a human observer. Automated test coverage (tui-config-test.sh) verifies all code paths programmatically, but the visual comparison against pre-change behavior cannot be performed in this non-interactive environment.

## Acceptance Provenance
No source requires a human sign-off for the automated portions. The tui-config-test.sh suite establishes all code-level assertions programmatically (catalogue ids, billing behavior, self-hosted picker, model row). The visual comparison step is the only non-automatable element, requiring a human at an interactive terminal.
